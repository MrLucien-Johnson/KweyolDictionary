# SQLite for local development (Phase 2 default)
DATABASE_URL="file:./prisma/dev.db"

# Public site URL used for metadata, sitemap and robots
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Admin sign-in (Phase 7). Change these before any shared deployment.
ADMIN_EMAIL="editor@example.com"
ADMIN_PASSWORD="changeme"
ADMIN_SESSION_SECRET="replace-with-a-long-random-secret"

# Production PostgreSQL example (Phase 2+ when deploying):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/kweyol_dictionary?schema=public"
